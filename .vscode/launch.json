{
    "configurations": [
      {
        "type": "debugpy",
        "request": "launch",
        "name": "Launch Program with Arguments",
        "program": "${workspaceFolder}/${input:programName}",
        "args": [
          "${input:programArgs}"
        ]
      }
    ],
    
  }